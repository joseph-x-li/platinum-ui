[package]
name = "platinum-showcase-server"
version = "0.1.0"
edition = "2021"
publish = false
description = "Static server for the platinum-ui showcase (trunk dist + SPA fallback)."

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "fs"] }
